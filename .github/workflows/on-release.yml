name: On Release

on:
  release:
    types:
      - released

jobs:
  publish:
    name: Publish Package
    uses: ./.github/workflows/callable-publish.yml
    permissions:
      id-token: write
      contents: read
